# Set default behavior to automatically normalize line endings
* text=auto

# Mark all Python files to ensure LF line endings
*.py text eol=lf

# Ensure JSON files are treated as text and normalized to LF line endings
*.json text eol=lf

# Ensure Markdown files are treated as text and normalized to LF line endings
*.md text eol=lf

# Ensure YAML files are treated as text and normalized to LF line endings
*.yml text eol=lf
*.yaml text eol=lf

# Ensure CSV files are treated as text and normalized to LF line endings
*.csv text eol=lf

# Ensure all shell scripts use LF line endings
*.sh text eol=lf

# Treat all files in docs directory as text files
docs/** text eol=lf

# Ensure Jupyter Notebooks are treated as binary
*.ipynb binary
